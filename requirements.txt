# Web framework — FastAPI as outer server, Uvicorn as ASGI runner
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Slack — Bolt for event handling, mounted on FastAPI via official adapter
slack-bolt>=1.18.0
slack-sdk>=3.27.0

# Google AI (Gemini Flash — new SDK)
google-genai>=1.0.0

# Instagram scraping (no API key needed)
instaloader>=4.10.3

# Web research (no API key needed)
ddgs>=0.1.0

# Google Sheets
gspread>=6.1.2
google-auth>=2.29.0

# Image validation
Pillow>=10.3.0

# HTTP downloads
requests>=2.31.0
httpx>=0.27.0

# Env vars
python-dotenv>=1.0.1

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.7
responses>=0.25.0
